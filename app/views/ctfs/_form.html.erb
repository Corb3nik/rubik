<%= form_for(ctf) do |f| %>
  <% if ctf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ctf.errors.count, "error") %> prohibited this ctf from being saved:</h2>

      <ul>
      <% ctf.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
